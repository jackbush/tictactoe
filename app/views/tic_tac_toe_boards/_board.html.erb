<div class="ttt-board">
  <% @game.board.each do |square| %>
    <% if square == 'x' %>
      <div class="ttt-square">x</div>
    <% elsif square == 'o' %>
      <div class="ttt-square">o</div>
    <% else %>
      <%= form_for(@game) do |f| %>
        <%= f.hidden_field :service, value: square %>
        <%= f.submit square, class: "ttt-square" %>
      <% end %>
    <% end %>
  <% end %>
</div>